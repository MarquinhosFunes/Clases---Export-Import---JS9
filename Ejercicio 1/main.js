// Importa las funciones sumar y restar desde math.js
import{sumar, restar} from'./math.js';

// Se le asigna valores a las variables
const a = 4; 
const b = 3;

// Muestra por consola la suma y resta
console.log("La suma es: ", sumar(a, b));
console.log("La resta es: ", restar(a, b));